[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E402",  # Ignore module-level import not at top of file (needed for sys.path configuration)
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
